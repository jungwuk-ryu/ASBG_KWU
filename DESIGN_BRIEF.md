# AWS Student Builder Group — 웹사이트 디자인 브리프

> 디자이너에게 그대로 전달하는 문서입니다.
> **구조(정보 구조 / 섹션 순서 / 콘텐츠 슬롯)는 확정**이고, **비주얼 언어는 전부 열려 있습니다.**

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 이름 | **AWS Student Builder Group** (약칭 **ASBG**) |
| 성격 | 대학생 AWS/클라우드 학습 동아리 홍보 + 리크루팅 랜딩 |
| 목표 | 방문자가 "이 동아리가 뭔지" 이해하고 **지원 폼으로 전환**하게 만드는 것 |
| 형태 | **원 페이지 스크롤** 사이트 (라우팅 없음, 내부 앵커 이동만) |
| 주 언어 | 한국어 (섹션 라벨은 영문 대문자 키워드 병기) |
| 대상 | 대학생, 클라우드 입문자 비중 높음 |
| 디바이스 | 데스크탑 우선 설계, **모바일 반응형 필수** |

**단 하나의 주요 CTA**: `지원하기` → 외부 폼(Tally/Google Form) 새 탭.
헤더와 히어로에 각각 1회, 스크롤 시 sticky 형태로 유지할지는 디자이너 판단.

---

## 2. 섹션 구성 (순서 확정)

### ① Nav (sticky)
- 좌: ASBG 로고 (**로고 신규 제작 필요**)
- 우: 앵커 링크 5개 — `소개` · `모집` · `활동` · `지난 기수` · `FAQ` + `지원하기` 버튼
- 모바일: 햄버거 → 풀스크린 오버레이

### ② Hero
- 상단 뱃지: `{N}th Recruiting` — **모집중임을 알리는 상태 표시**(펄스/글로우 등 살아있는 느낌)
- H1: `AWS Student Builder Group {N}기 멤버 모집`
- 서브카피 1줄 (선택)
- 주 CTA 버튼: `지원하기`
- 배경: 시각적으로 가장 강한 구간. 추상 도형 / 그라디언트 / 애니메이션 등 자유

### ③ ABOUT US — 소개
- 섹션 라벨 `ABOUT US`
- 본문 2문단
  - 1문단: ASBG가 어떤 동아리인지 (AWS 기반 학생 빌더 커뮤니티)
  - 2문단: 국내에서 어떻게 운영되는지 (세션, 커리어 토크, 프로젝트 등)
- 이미지 없음. **타이포그래피만으로 버티는 구간** — 여백과 문장 리듬이 핵심

### ④ RECRUITING — 리크루팅 일정
- 섹션 라벨 `RECRUITING` / H2 `리크루팅 일정`
- **6행 타임라인**, 각 행 = `단계명` + `날짜/시간`, 행 사이 divider

| 단계 | 예시 값 |
|---|---|
| 서류 접수 | 3/1(일) ~ 3/11(수) |
| 서류 결과 안내 | 3/15(일) 21시 |
| 인터뷰 | 3/17(화) ~ 3/19(목) |
| 최종 결과 안내 | 3/23(월) 21시 |
| 교내 이벤트 참여 | 3/25(수) 18:00 ~ 21:00 |
| Welcome Party | 4/1(수) 18:30 ~ 20:00 |

> 디자인 포인트: 단순 표로 두지 말고 **진행 흐름이 읽히는 형태** 권장 (세로 스텝, 진행선 등). 현재 단계 하이라이트가 가능하면 좋음.

### ⑤ FUTURE — 이번 기수 예정 활동
- 섹션 라벨 `FUTURE` / H2 `{N}기 예정 활동`
- **카드 3개**, 각 카드 = `제목` + `설명 2~4줄`
  1. **정기 세션** — 회차별 커리큘럼 (입문 핸즈온 → 자유 발표)
  2. **소모임** — 프로젝트 / 취업준비 / 취미 등 자율 개설
  3. **연합 세션** — 타 학교 교류, 현업 엔지니어 초청

> 카드 1은 회차 목록(3줄)이 들어가 다른 카드보다 길다. **높이 불균형을 어떻게 처리할지** 디자인 결정 필요.

### ⑥⑦⑧ PAST — 지난 기수 활동 (3개 섹션 반복)
- 섹션 라벨 `PAST` / H2 `3기 활동` · `2기 활동` · `1기 활동`
- 각 기수 안에 활동 항목 N개, **항목 1개 = `카테고리 뱃지` + `제목` + `설명 2~4줄` + `사진 1장`**

**ASBG는 1기 ~ 4기, 총 4개 기수**의 이력이 있습니다. 기수당 활동 항목은 3~5개 예상.

> ⚠️ **가장 중요한 디자인 결정**: 원본(3개 기수)은 기수마다 레이아웃이 전부 달랐습니다(그리드형/카드형/리스트형). 같은 성격의 콘텐츠가 세 번 다른 얼굴로 나와 페이지가 산만해집니다. ASBG는 기수가 **4개**라 그대로 쌓으면 페이지가 과도하게 길어집니다.
>
> **권장**: 반복 가능한 **활동 카드 컴포넌트 1종**으로 통일하고, 기수 구분은 **탭/토글**로 처리. 페이지 길이가 1/4로 줄고 기수가 늘어나도 구조가 유지됩니다.
>
> 항목 수가 기수마다 3~5개로 다르므로 **개수에 무관하게 깨지지 않는 그리드**여야 합니다.

> 📌 **콘텐츠는 나중, 디자인은 지금**: 지난 기수 사진·설명 텍스트는 추후 수급 예정입니다. 하지만 **활동 카드와 기수 탭 디자인은 이번 작업에 반드시 포함**되어야 합니다. 더미 콘텐츠 기준으로 설계해 주세요. 카드는 다음 상태를 모두 커버해야 합니다:
> - 사진 있음 / 사진 없음 (사진 미수급 항목 대비)
> - 설명 2줄 / 5줄 (길이 편차 흡수)
> - 한 기수에 항목 3개 / 5개

### ⑨ FAQ
- 섹션 라벨 `FAQ` / H2 `자주 들어오는 질문`
- **아코디언 6문항** (기본 전체 닫힘, 클릭 시 펼침)
  1. 리크루팅 과정에서의 구체적인 선발 기준이 궁금해요.
  2. 제출한 지원서를 수정하고 싶어요. / 중복 제출해도 괜찮을까요?
  3. 활동은 주로 어느 요일, 몇 시, 어디에서 진행하나요?
  4. 다른 대외활동이나 취업 준비와 병행 가능한가요? 시간·에너지가 얼마나 드나요?
  5. 활동 수료증 혹은 인증서를 받을 수 있나요?
  6. ASBG {N}기 운영진이 궁금해요.
- 답변은 문항당 2~5줄. 6번은 운영진 프로필(사진+이름+역할)이 들어갈 수 있음

### ⑩ Footer
- ASBG 로고 + 한 줄 소개
- 인스타그램 등 SNS 아이콘
- `지원하기` 링크 1회 더

---

## 3. 디자인 방향 (열린 영역)

### 반드시 정해주셔야 하는 것
1. **로고** — ASBG 워드마크 + 심볼, 라이트/다크 양쪽
2. **컬러 시스템** — 배경 / 표면 / 텍스트 3단계 / 액센트 / 보더
3. **타이포 스케일** — 한글 본문 가독성이 최우선. 영문 라벨용 서체 별도 지정 가능
4. **컴포넌트 3종** — 버튼(주/보조), 활동 카드, 아코디언 행
5. **섹션 라벨 스타일** — `ABOUT US` / `RECRUITING` 같은 작은 영문 키워드의 취급 방식

### 참고 정보
- AWS 브랜드 인접: Squid Ink `#232F3E`, Smile Orange `#FF9900`
- 참고한 원본(ACC KWU)은 **다크 + 보라 액센트**(`#04070d` 배경, `#df7afe`/`#814ac8` 포인트)였습니다. **의도적으로 다르게 가는 것이 이번 작업의 목적**이므로 이 팔레트는 피해주세요.
- "빌더(Builder)"라는 이름값 — 만드는 사람들의 커뮤니티라는 성격이 비주얼에 드러나면 좋음

### 제약
- 텍스트 대비 WCAG AA 이상
- 애니메이션은 `prefers-reduced-motion` 대응
- 활동 사진은 **현장 스냅 사진**이라 색감·비율이 제각각. 카드 디자인이 이를 흡수해야 함 (일관된 crop 비율, 오버레이 등)

---

## 4. 모션 / 인터랙션

원본과 유사한 "살아있는" 느낌을 목표로 합니다. 원본에서 확인된 것 + 권장 사항:

| 대상 | 동작 |
|---|---|
| Hero 배경 도형 | 로드 시 `scale 0.7 → 1`, `opacity 0 → 1`, **1.6s** / 큰 원은 `scale 0.5 → 1`, **1.8s**. 커스텀 이징 `cubic-bezier(.67,.33,.23,1.02)` |
| 모집중 뱃지 | 점(dot)이 무한 펄스 — 확산하는 링 + 페이드 |
| 섹션 진입 | 뷰포트 진입 시 fade + 살짝 위로 (y 16~24px), 자식 요소 stagger 60~80ms |
| 타임라인 행 | 위에서 아래로 순차 등장 |
| 활동 카드 | hover 시 살짝 떠오름 / 보더·글로우 강조 |
| FAQ 아코디언 | 높이 + opacity 트랜지션, 아이콘 회전 |
| 버튼 | hover 글로우 또는 스케일, active 눌림 |
| 스크롤 | 부드러운 앵커 스크롤 |

- 전 구간 `prefers-reduced-motion: reduce` 대응 필수 (모션 제거, 최종 상태만 표시)
- 스크롤 하이재킹·패럴랙스 과용은 지양. 모바일에서 무거워짐

---

## 5. 기술 스택 (구현 참고)

- **Astro** 정적 사이트 + **motion** (vanilla, Framer Motion 계열) — 원본이 Framer Motion 기반이라 모션 재현이 자연스러움
- 콘텐츠는 **content collection**으로 분리 (활동 항목 = md/json 1개 + 이미지 1장을 폴더에 드롭)
- **별도 백오피스/CMS 없음.** 학기당 수 회 업데이트라 파일 편집으로 충분. 비개발자 운영진 인수인계가 필요해지면 그때 git 기반 CMS(Sveltia/Decap)를 얹으면 되고, 콘텐츠 구조는 그대로 재사용됨
- 배포: Cloudflare Pages 또는 Vercel (무료)

---

## 6. 미정 항목 (콘텐츠 확정 필요)

**확정됨**
- 기수: **5기**
- 인스타그램: **@aws.sbg.kwu**
- 지원 폼: **https://tally.so/r/dWGWJr**
- 리크루팅 일정: **서류 접수 8/21(금)~9/6(일) · 서류 결과 9/7(월) · 인터뷰 9/9(수)~9/11(금) · 최종 결과 9/15(화)**

**대기 중** (placeholder로 두고 나중에 교체)
- [ ] 교내 이벤트·Welcome Party 실제 날짜
- [ ] 지난 기수 활동 사진 (원본 사이트 사진은 ACC KWU 소유 — **재사용 불가, 자체 사진 필요**)
- [ ] FAQ 답변 본문 6개
- [ ] 운영진 명단/사진
- [ ] ASBG 자체 기수 이력 — 원본은 1~3기 활동이 있으나 ASBG에 해당 이력이 있는지 확인 필요. 없다면 PAST 섹션 구성 재검토

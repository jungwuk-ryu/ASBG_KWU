.DS_Store
AGENTS.md
.wrangler/
AWS Student Builder 웹사이트/uploads/

# 활동 사진은 배포에만 포함하고 초상권 보호를 위해 Git에는 기록하지 않습니다.
AWS Student Builder 웹사이트/assets/photos/
dist/assets/photos/

# dist/ 는 커밋합니다 — Cloudflare 빌드 환경이 python3 로 build.py 를 돌리지 않고
# `npx wrangler deploy` 만 실행하므로, 배포 시점에 dist/ 가 저장소에 있어야 합니다.
